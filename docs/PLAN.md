# 总体方案

> 稳定文档：只在方向变化时更新。执行细节写在 `HANDOFF.md`。

## 目标

在本仓库建立 **Codex + Grok** 双 Agent 协作流程，后续业务功能都在此目录下迭代。

## 原则

1. 同一本地路径，两边打开同一文件夹。
2. 用 `HANDOFF.md` 做交战报告，用 Git 做边界。
3. Grok 侦察/验收；Codex 主实现；用户拍板。

## 阶段

### 阶段 0 — 基建（当前）

- [x] 创建工作区 `D:\ai沙盒\codex+grok`
- [x] 添加 README / AGENTS / PLAN / HANDOFF
- [ ] 初始化 Git（若尚未初始化）
- [ ] 用户确认第一项真实业务目标

### 阶段 1 — 业务开发

- 由用户写入具体产品/功能目标
- 拆任务 → Codex 实现 → Grok 验收 → 循环

## 技术选型

（业务确定后填写：语言、框架、运行方式等）

## 风险与注意

- 免费 Cursor 不能当 Grok 4.5 主力；编码优先 Codex 桌面版或 Grok Build。
- 避免两边同时改同一文件导致覆盖。
