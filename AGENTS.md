# 项目约定（Codex 与 Grok 共用）

## 工作区

- 根目录：`D:\ai沙盒\codex+grok`
- 交接文件：`docs/HANDOFF.md`（每次换手必读必写）
- 方案文件：`docs/PLAN.md`（总体方案，少改）

## 协作规则

1. 改代码前先读 `docs/HANDOFF.md`。
2. 只做 HANDOFF「未完成」中明确列出的项；范围外先写进 HANDOFF 再问用户。
3. 完成一项就勾选，并在「最近变更」写清改了哪些文件。
4. 有 Git 时：小步 commit，信息用中文或英文均可，说清楚做了什么。
5. **禁止**提交密钥、Token、`.env`、账号凭据。

## 分工建议

- **Codex**：功能实现、大范围重构、页面/文档生成。
- **Grok**：环境排查、命令验证、diff 审查、跨工具配置。
- **用户**：需求拍板与最终验收。

## 编码偏好（可随项目改）

- 优先清晰可读，少炫技。
- 改动尽量小、可回滚。
- 新增依赖前先在 HANDOFF 说明理由。

## 禁区（默认）

- `**/.env*`
- `**/auth.json`
- `**/*secret*`
- 用户未备份的大资源/生产数据
